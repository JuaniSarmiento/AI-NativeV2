# Resumen Consolidado — 05-dx

> 6 archivos. Última actualización: 2026-04-13

---

## 01_onboarding.md
- Guía paso a paso para setup del entorno de desarrollo
- Docker Compose, DB setup, seed data, running tests

## 02_tooling.md
- Comandos de pytest, alembic, docker compose
- psql queries útiles para debugging

## 03_trampas_conocidas.md
- 12 secciones de gotchas: SQLAlchemy async, Alembic multi-schema, FastAPI, Zustand 5, React 19, WebSockets, Hash Chain CTR, Sandbox, Pydantic v2, PostgreSQL, Anthropic API, Testing Async
- Contenido muy valioso para desarrollo — referencia antes de codear

## 04_convenciones_y_estandares.md
- Naming: archivos snake_case, clases PascalCase, vars snake_case, endpoints kebab-case
- URLs: /api/v1/{resource-plural}/{id}/{sub-resource}
- HTTP methods, status codes, paginación, error codes
- Modelos SQLAlchemy: __tablename__ snake_case plural, soft delete via is_active, CTR inmutable
- Git: conventional commits, feature branches
- Imports ordenados: stdlib → 3rd party → app
- Tests: naming test_{what}_{condition}_{expected}

## 05_workflow_implementacion.md
- Workflow paso a paso para implementar features
- Desde OPSX hasta merge

## 06_estrategia_de_testing.md
- Pirámide: unit → integration → adversarial → E2E
- Coverage mínimo 80%, hash_chain 100% (target independiente)
- Tests adversariales del tutor (20+ tests anti-solver)
- Backend: pytest + httpx, Frontend: Vitest + Testing Library, E2E: Playwright

---

## INCONSISTENCIAS (RESUELTAS)

### IC-DX1: hash_chain.py ubicación
- Referencias históricas divergentes (core vs feature) ya no aparecen en la documentación base.
- **Decisión vigente**: `app/features/cognitive/hash_chain.py` (pertenece al dominio cognitive, no a core).

### IC-DX2: Endpoint examples en convenciones usan estructura REST para tutor
- El ejemplo REST para tutor se considera confuso; el tutor es **WebSocket** (streaming).
- **Estado**: resuelto (la documentación base ya se centra en `WS /ws/tutor/chat`).

### IC-DX3: hash_chain import path en tests
- **Decisión vigente**: usar `HashChainService` (clase) e importar desde `app.features.cognitive.hash_chain`.

---

## FIXES NECESARIOS

- Ninguno pendiente (items anteriores cerrados).
